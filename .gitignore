# Nix / direnv
result
result-*
.direnv/

# Zig build outputs (per-source tree)
sources/*/zig-out/
sources/*/zig-cache/
sources/*/.zig-cache/
sources/*/build/

# Python build outputs / caches
__pycache__/
*.pyc
*.pyo
*.egg-info/
sources/*/.dist/
sources/*/dist/
sources/*/build/
sources/*/.pytest_cache/
sources/*/.tox/
sources/*/.venv/

# abuild scratch + outputs
.abuild/
main/*/src/
main/*/pkg/
*.apk
APKINDEX.tar.gz

# Private signing keys — only .rsa.pub belongs in git
*.rsa
!keys/*.rsa.pub

# Claude Code session-local state
.claude/
